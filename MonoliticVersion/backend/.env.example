# Environment variables for Flask backend
SECRET_KEY=your-super-secret-key-change-in-production
FLASK_DEBUG=True
FLASK_ENV=development
PORT=5000
HOST=0.0.0.0
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=16777216  # 16MB in bytes

# Database configuration (PostgreSQL)
DATABASE_URL=postgresql://musicapp_user:musicapp_password@localhost:5432/musicapp
DB_HOST=localhost
DB_PORT=5432
DB_NAME=musicapp
DB_USER=musicapp_user
DB_PASSWORD=musicapp_password

# Para desarrollo local con SQLite (comentar las líneas de PostgreSQL arriba):
# DATABASE_URL=sqlite:///musicapp.db
